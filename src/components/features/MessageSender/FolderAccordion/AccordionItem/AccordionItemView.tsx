import React from 'react';
import { AccordionItemPropsType } from './type';

const AccordionItemView = ({ children }: AccordionItemPropsType) => {
	return <div>{children}</div>;
};

export default AccordionItemView;
