import React from 'react';
import { AccordionPanelPropsType } from './type';

const AccordionPanelView = ({ children }: AccordionPanelPropsType) => {
	return <div>{children}</div>;
};

export default AccordionPanelView;
