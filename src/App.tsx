import React from 'react';
import { Routers } from '@/components/shared';
import { Layout } from '@/components/layout';

const App = () => {
	return (
		<Layout>
			<Routers />
		</Layout>
	);
};

export default App;
